{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pet",
  "description": "Capability set for the pet companion window (events + dragging)",
  "windows": ["pet"],
  "permissions": [
    "core:default",
    "core:webview:default",
    "core:window:allow-start-dragging"
  ]
}
