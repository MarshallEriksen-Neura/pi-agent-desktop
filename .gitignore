# dependencies
/node_modules
apps/*/node_modules
packages/*/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# next.js auto-generated
next-env.d.ts

# production
/build
/.tmp/
/packages/*/dist/
apps/*/dist

# mobile app (Capacitor)
# Android project source IS committed (MainActivity, Manifest, Gradle config).
# Build artifacts + local SDK paths are excluded by apps/mobile/android/.gitignore.
# iOS is deferred to macOS — not committed yet.
apps/*/ios
apps/*/.vite

# tauri / rust
/src-tauri/target
/src-tauri/gen
# defensive: any crate-level cargo build output
target/

# typescript
*.tsbuildinfo
next-env.d.ts.timestamp

# env / secrets
.env
.env*.local
*.pem

# stitch API key (referenced via {file:.stitch-key} in opencode.json)
.stitch-key

# figma MCP bearer token (referenced via {file:.figma-token} in opencode.json)
.figma-token

# pi CLI local state (sessions, cache)
.pi/

# updater signing keys (local only; pubkey lives in tauri.conf.json)
src-tauri/.tauri-keys/

# logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / os
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# agent / tool metadata (local only, not project source)
.firecrawl/
.ragcode/
.omc/
.omx/
.claude/
.comate/
.agents/
skills-lock.json
.trae/